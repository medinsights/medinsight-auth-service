apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: medinsights

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - postgres-statefulset.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: auth-service
  app.kubernetes.io/part-of: medinsights
  app.kubernetes.io/managed-by: kustomize
